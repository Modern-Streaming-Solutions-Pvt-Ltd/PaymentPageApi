version: 0.2
phases:
  install:
    runtime-versions:
      dotnet: 8.0
  pre_build:
    commands:
      - echo Restore started on `date`
      - dotnet restore PlanPaymentPage.csproj
  build:
    commands:
      - echo Build started on `date`
      - dotnet publish -c release --no-restore -o ./PlanPaymentPage/build_output PlanPaymentPage.csproj /p:EnvironmentName=Production
      - 'pwd'
artifacts:
  files: 
    - PlanPaymentPage/**/*
    - scripts/**/*
    - appspec.yml
# - 'dir ./build-output'